# P3 — Learning engine, SRS, sessions and statistics

> Detailed work packages for `MASTER_PLAN.md`. `SPEC_V1.md` remains normative.

## P3.1 — ReviewEvent audit log and progress projection

**Status:** IMPLEMENTED — verification pending.

**Spec:** §28, §30, §33, §57, §133

### Deliverables
- [x] Implement canonical review_events with policy/dataset/normalization versions, snapshots, latency semantics and signal quality.
- [x] Implement progress as a rebuildable materialized projection keyed by profile/track/CardDefinition.
- [x] Persist UTC plus local date/timezone/offset history.

### Exit criteria
- [x] Events are append/audit source; progress can be rebuilt from historical post-state snapshots. stats_daily remains P3.13 scope.
- [x] Notification latency never substitutes for cognitive answer latency.

## P3.2 — Introduction and learning-step state machine

**Spec:** §3.1, §28, §29, §30, §50

### Deliverables
- [ ] New cards enter explicit introduction/exposure before retrieval.
- [ ] Schedule first retrieval after interleaving/learning steps; never score first exposure as failure.
- [ ] Prevent immediate retest after failure and respect relearning steps.

### Exit criteria
- [ ] First encounter cannot become a false SRS failure.
- [ ] Learning/relearning are distinct from long review queue.

## P3.3 — ReviewPolicy V1 core

**Spec:** §29, §30, §96

### Deliverables
- [ ] Implement deterministic versioned boxes/intervals, elapsed-time adjustment, deterministic jitter and difficulty_factor bounds.
- [ ] Implement relapse demotion, review/relearning transitions and mastery as non-terminal derived label.
- [ ] Use injectable Clock; no datetime.now() in domain logic.

### Exit criteria
- [ ] Identical history/policy yields reproducible state.
- [ ] Overdue remembered cards are not artificially shortened.
- [ ] Mastery decays with time but scheduling continues.

## P3.4 — Verified-retrieval gate and signal weighting

**Spec:** §3.1, §3.2, §29, §30, §40, §43, §133

### Deliverables
- [ ] Distinguish exposure, post-retrieval self-assessment, verified MCQ/free-text/cloze and exam retrieval.
- [ ] Enforce verified_gate_box and periodic verified retrieval.
- [ ] Reduce confidence for hint_used and shared-device responses.
- [ ] Keep notification delivery/action latency out of SRS strength.

### Exit criteria
- [ ] Visible-answer self-assessment cannot promote long boxes.
- [ ] A shared-device answer alone cannot cross verified gate unless explicitly trusted.

## P3.5 — Sibling burial, prerequisites and confusable introduction spacing

**Spec:** §15, §29, §52, §76.1, §96

### Deliverables
- [ ] Enforce new/review sibling gaps and prerequisite unlocks.
- [ ] Keep confusable distractors/groups away from new/learning/relearning introductions.
- [ ] Prevent priming-driven false success across sibling CardDefinitions.

### Exit criteria
- [ ] Sibling and prerequisite behavior is deterministic/tested.
- [ ] Confusable training activates only when cards are sufficiently stable.

## P3.6 — Quiz engine, distractors and corrective feedback

**Spec:** §3.2, §11, §51, §52

### Deliverables
- [ ] Implement MCQ 4–6 panel options and cloze-MCQ for grammar.
- [ ] Sample distractors from indexed pools; exclude native-concept equivalents, accepted normalized answers, explicit synonyms and relevant sibling answers.
- [ ] Balance answer positions, rotate examples deterministically and show corrective/contrastive feedback outside exams.

### Exit criteria
- [ ] No ORDER BY RANDOM() on large corpora.
- [ ] I-don't-know remains explicit and never rewards guessing.
- [ ] Grammar difficulty is not accidentally vocabulary-reading difficulty.

## P3.7 — Panel free-text grading and content-quality feedback

**Spec:** §8, §17, §30, §51, §66

### Deliverables
- [ ] Implement exact/any_of/fuzzy_normalized by supported script/policy.
- [ ] Represent correct/wrong/unrecognized; unrecognized does not auto-penalize SRS.
- [ ] Implement 'Ma réponse devrait être acceptée' and content/report workflow.

### Exit criteria
- [ ] Potentially valid unknown answer is recoverable without false failure.
- [ ] Normalization_version is persisted with grading event.

## P3.8 — Persistent sessions, concurrency and cross-client resume

**Spec:** §3.3, §53, §54, §66

### Deliverables
- [ ] Persist session/question state and strategy/settings.
- [ ] Implement CAS answer with expected_version and stale_session.
- [ ] Implement session subscribe, pause/complete/undo boundaries and resume from another client.

### Exit criteria
- [ ] Exactly one concurrent answer mutation wins.
- [ ] Session can resume after reconnect/client switch.

## P3.9 — Fatigue-aware session selection and interleaving

**Spec:** §27, §30, §53

### Deliverables
- [ ] Interleave available content types subject to due/relearning/sibling/weights.
- [ ] Avoid new cards in final quarter of bounded sessions.
- [ ] Detect recent verified-accuracy drop and offer finish/recognition-only/continue choices.

### Exit criteria
- [ ] Fatigue mitigation never silently rewrites SRS history.
- [ ] Selection remains explainable and testable.

## P3.10 — Known-already, suspend/bury and calibration

**Spec:** §28, §129

### Deliverables
- [ ] Implement known_already/suspended/buried/suspend_until independently from content lifecycle.
- [ ] Add optional initial calibration sample to skip trivial content without pretending it was reviewed.

### Exit criteria
- [ ] User state remains distinct from active/removed/superseded content state.

## P3.11 — Leech detection, confusion matrix and personal annotations

**Spec:** §31, §31.1, §52, §58

### Deliverables
- [ ] Implement versioned leech thresholds and reduced automatic frequency.
- [ ] Track expected/chosen answer confusion counts.
- [ ] Add private/exportable annotations on LearningItem or CardDefinition and prioritize mnemonic remediation.

### Exit criteria
- [ ] Leech remediation does not simply increase repetition.
- [ ] Personal notes are profile-private unless explicitly shared.

## P3.12 — Undo, integrity rebuild and algorithmic recompute

**Spec:** §32, §33, §57, §66

### Deliverables
- [ ] Persist pre/post state snapshots per mutation.
- [ ] Implement safe undo via snapshot/compensating event.
- [ ] Separate integrity rebuild under historical policies from deliberate recompute under target policy; rebuild stats independently.

### Exit criteria
- [ ] Undo never naïvely decrements counters.
- [ ] Policy migrations are explicit operations with divergence report.

## P3.13 — Basic statistics and metacognitive calibration backend

**Spec:** §57, §58

### Deliverables
- [ ] Build stats_daily projection and core due/verified accuracy/retention/new-review-relearning/leech metrics.
- [ ] Compute self-assessed-known vs later verified performance and confusion views.
- [ ] Implement streak semantics with due-queue goal, grace day and neutral no-due days.

### Exit criteria
- [ ] Dashboard metrics distinguish exposure/self-assessment from verified retrieval.
- [ ] Historical local dates are not rewritten when timezone changes.

## P3.14 — Long-horizon SRS simulation gate

**Spec:** §29, §35, §80, §96, §129

### Deliverables
- [ ] Simulate synthetic learners over ~180 days across presets and error patterns.
- [ ] Detect due-queue explosion, relearning oscillation, starvation, over-promotion and unrealistic workload.
- [ ] Tune defaults only with documented simulation evidence.

### Exit criteria
- [ ] Standard defaults remain sustainable under declared synthetic scenarios.
- [ ] Simulation is deterministic/reproducible in CI or scheduled quality job.

## P3 exit gate

- [ ] Cards can be introduced, retrieved, graded, scheduled, undone and rebuilt deterministically; long-horizon SRS remains sustainable.
